# Taskboard

Internal kanban-style task tracker for small teams.

## Packages

- `packages/api` — Express REST API (TypeScript)
- `packages/web` — React SPA
- `packages/shared` — shared types and validation schemas

## Quick start

```bash
npm install
npm run build
npm run test
```

API runs on port 3000, web dev server on 5173.
